### 
1. Database model with SQLAlchemy
2. Prepare mock data
3. Setup redis
4. Prepare the ingestion pipeline ? Upload of the dataset to use and normalize. Connect with a database, aws S3? Identify structure of dataset first ? 
5. Authentication ? 
6. Define the REST endpoints:
   * endpoint/projects. Get active and inactive projects.
   * endpoint/project. Get one project info.
   * endpoint/project/{projectId}/status ? 